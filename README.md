# Shikimori-Hama.bundle

Агент метаданных для Plex, ориентированный на русскоязычное сообщество Shikimori.

Этот проект является форком [Hama.bundle](https://github.com/ZeroQI/Hama.bundle) от ZeroQI.  
Основная цель форка — полный переход на современный GraphQL API Shikimori и предоставление наиболее точных и актуальных данных для русскоязычных пользователей Plex.

---

## 🚀 Основные возможности

- ✨ **Полная интеграция с GraphQL API Shikimori**  
  Отказ от устаревших и нестабильных HTML-парсеров в пользу официального API.

- 🔗 **Интеграция с ARM API**  
  Автоматическое получение Shikimori ID по AniDB ID вашего файла, что обеспечивает высокую точность совпадений.

- 🇷🇺 **Фокус на русскоязычных данных**  
  Приоритетное получение ключевой информации для комфортного просмотра:  
  - Название на русском языке  
  - Описание, очищенное от HTML и BBCode  
  - Список жанров на русском языке

- 🖼️ **Улучшенная работа с постерами TMDb**  
  - Механизм обхода кэша API ("cache buster")
  - Поддержка постеров сезонов
  - Умная сортировка по языку, разрешению и рейтингу

---

## 🔧 Установка и настройка

1. Установите плагин в Plex Media Server, следуя стандартной инструкции.
2. Перейдите в настройки вашей аниме-библиотеки в Plex.
3. Во вкладке **"Дополнительно" (Advanced)** выберите агент **Hama.bundle**.

---

## ⚙️ Настройки агента TheMovieDb

### TMDb Poster Languages

**Настройка:** `TMDbPosterLanguages`

Указывает, для каких языков запрашивать постеры из TMDb. Используется параметр API `include_image_language`.

> **⚠️ ВАЖНО:** Языки пишутся **в одну строку через запятую**, без пробелов!

**Формат:** `ru,en,null`

#### Коды языков

TMDb использует стандарт **ISO 639-1** для языковых кодов. Вот наиболее полезные коды для аниме:

| Код | Язык | Примечание |
|-----|------|------------|
| `ru` | Русский | 🇷🇺 |
| `en` | Английский | 🇬🇧 Чаще всего доступен |
| `ja` | Японский | 🇯🇵 Оригинальные постеры |
| `ko` | Корейский | 🇰🇷 |
| `zh` | Китайский | 🇨🇳 |
| `uk` | Украинский | 🇺🇦 |
| `de` | Немецкий | 🇩🇪 |
| `fr` | Французский | 🇫🇷 |
| `es` | Испанский | 🇪🇸 |
| `it` | Итальянский | 🇮🇹 |
| `pt` | Португальский | 🇵🇹 |
| `pl` | Польский | 🇵🇱 |
| `null` | Без языка | Универсальные арты |

> **📖 Полный список:** TMDb поддерживает более 180 языков. Полный список доступен через API: `https://api.themoviedb.org/3/configuration/languages`

#### Примеры настройки

| Настройка | Что будет загружено |
|-----------|---------------------|
| `ru,en,null` | Русские → Английские → Без текста |
| `ru,ja,null` | Русские → Японские → Без текста |
| `ru,null` | Только русские и универсальные |
| `en,null` | Только английские и универсальные |
| `null` | Только постеры без текста |
| `all` | Все доступные постеры |

#### Как это работает технически

При запросе изображений агент формирует URL вида:
```
https://api.tmdb.org/3/tv/{id}/images?include_image_language=ru,en,null
```

- Если указаны конкретные языки — к запросу добавляется **"cache buster"** (метка времени), чтобы обойти кэш TMDb API
- Если указано `all` или поле пустое — фильтрация по языку отключается, возвращаются все изображения

---

### Poster Language Priority

**Настройка:** `PosterLanguagePriority`

Определяет приоритет языков при сортировке найденных постеров. Постеры с языком, указанным раньше в списке, будут отображаться первыми.

**Формат:** `ru,en,null` (в одну строку через запятую)

---

### Force TMDb Poster Refresh

**Настройка:** `force_tmdb_poster_refresh`

Если включена, полностью отключает кэширование списка изображений агентом. Полезно, если на TMDb были добавлены новые постеры и нужно их немедленно получить.

| Значение | Описание |
|----------|----------|
| `true` | Кэш отключён, каждый запрос идёт к API |
| `false` | Используется стандартное кэширование (1 неделя) |

---

### Prioritize Season Poster

**Настройка:** `prioritize_season_poster`

Если включена и сериал состоит из одного сезона, постер этого сезона будет использоваться как главный постер сериала. Особенно полезно для одно-сезонных аниме.

---

## ⚠️ Важно: Настройка источника метаданных

Для корректной работы плагина необходимо, чтобы Shikimori был главным источником метаданных.
 
Убедитесь, что Shikimori включен и находится на первом месте в списке приоритетов.

---

## 🔍 Принцип работы

Процесс получения метаданных полностью автоматизирован и состоит из трёх шагов:

1. **Поиск Shikimori ID**  
   Агент использует AniDB ID вашего файла для запроса к ARM API, чтобы найти соответствующий Shikimori ID.

2. **Запрос метаданных**  
   Полученный Shikimori ID используется для отправки GraphQL-запроса к API:  
   `https://shikimori.one/api/graphql`

3. **Обработка и вывод**  
   Плагин получает данные (название, описание, жанры), обрабатывает их и формирует готовые метаданные для вашей библиотеки Plex.

---

## 🖼️ Принцип работы с постерами TMDb

1. **Поиск TMDb ID через ARM API**  
   Агент ищет TMDb ID по AniDB ID через ARM API (приоритетный метод).

2. **Запрос изображений**  
   Формируется запрос к TMDb Images API с учётом настроек языка и cache buster.

3. **Сортировка и выбор**  
   Постеры сортируются по:
   - Языку (согласно `PosterLanguagePriority`)
   - Разрешению (больше — лучше)
   - Рейтингу на TMDb

---

## 🤖 Совместимость с ботом-синхронизатором

Полностью совместим с Telegram-ботом для автоматической синхронизации просмотров между Plex и Shikimori.

Это обеспечивает полный цикл автоматизации: от точного получения метаданных до автоматической отметки просмотренных серий в вашем профиле Shikimori.

⚠️ Бот находится в активной разработке. Подробности будут доступны в его репозитории.  
Ссылка на репозиторий бота будет добавлена позже.

---

## 🛠️ Технические детали

### GraphQL-запрос к Shikimori

```graphql
query getAnimeData($id: String!) {
  animes(ids: $id) {
    id
    russian
    description
    genres {
      russian
    }
  }
}
```

### ARM API для конвертации ID

```
https://arm.haglund.dev/api/v2/ids?source=anidb&include=themoviedb&id={AniDB_ID}
```

---

## 📁 Различия с оригинальным Hama.bundle

| Компонент | Оригинал (Hama.bundle-master) | Этот форк (Shikimori-Hama.bundle) |
|-----------|-------------------------------|-----------------------------------|
| `Shikimori.py` | ❌ Отсутствует | ✅ Новый модуль с GraphQL API |
| `TheMovieDb.py` | Базовая версия (148 строк) | Расширенная версия (253 строки) |
| ARM API | ❌ Не используется | ✅ Для поиска TMDb и Shikimori ID |
| Cache Buster | ❌ Нет | ✅ Обход кэша API |
| Сезонные постеры | ❌ Нет | ✅ Полная поддержка |
| Сортировка постеров | По языку и рейтингу | По языку, разрешению и рейтингу |

---

## 📜 Лицензия

Этот проект распространяется под той же лицензией, что и оригинальный Hama.bundle.
